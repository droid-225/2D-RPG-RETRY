// Change log / tutorial
// Future me please for the love of god think of this as a change log
// Both of us know how much Benny changes old shit
// Look at the program while you parse through this
Video 1: 12/14/2022
//Setup
- Create new project
- Make copy of  LWJGL library folder from Programmin\Java to project folder 
- Right click on project folder
-> Build path
--> Add libraries...
---> User Library
----> User Libraries
-----> New...
------> Add JARs... (only if  LWJGL lib was copied to project folder)
-------> Navigate to lib folder and add JARs...
--------> Add library
---------> apply and close
- Right click project folder
-> Build path
--> Configure Build Path
---> Run/Debug Settings
----> New...
-----> Java Application
------> VM arguments (might need to redo this after main class is made)
-------> Enter -Djava.library.path=""C:\Users\aryan\eclipse-workspace\2D_RPG_RETRY\lib\natives"
-------> Apply and Close
//End of Setup

- Create new package for game engine
- Create main class in engine package
- Create main method
-> Write "initDisplay();"
- Create initDisplay method (private static void initDisplay())
-> Write Display.setDisplayMode(new DisplayMode(800,600));
// import any imports as they come up (usually from lwjgl)

-> Write Display.create();
-> Write Keyboard.create();
-> Surround above code in try catch block
--> In catch block Write Logger.getLogger(Main.class.getName()).log(Level.SEVERE,  null, ex); and set its condition to LWJGLException ex

- Goto main method
-> Write initGl();

- Create initGL method
- import static org.lwjgl.opengl.GL11.*;
- Goto initGL method
-> Write glMatrixMode(GL_PROJECTION);
-> Write glLoadIdentity();
-> Write glOrtho(0,Display.getWidth(),0,Display.getHeight(),-1,1);
-> Write glMatrixMode(GL_MODELVIEW);
-> glDisable(GL_DEPTH_TEST); // since we're doing 2D
-> glClearColor(0,0,0,0);

- Goto main method
-> Write gameLoop();

- Create gameLoop method
-> Create while loop with condition !Display.isCloseRequested()
--> Write getInput(); // all three of these are inside the while loop, hence the '-->'
--> Write update();
--> Write render();

- Move initGL method above gameLoop method

- Create getInput method // these 3 methods are below intiGL and above gameLoop methods
- Create update method
- Create render method // following writes are in render method
-> Write glClear(GL_COLOR_BUFFER_BIT); // clears screen
-> glLoadIdentity();
-> Write //draw <- that is a comment in the code itself (placeholder)
-> Write Display.update();
-> Write Display.sync(60); // runs game at 60 fps

- Goto initDisplay method
-> Write Display.setVSyncEnabled(true); // in try

- Goto main method
-> Write cleanUp();

- Create cleanUp method //above initDisplay method
-> Write Display.destory(); 
-> Write Keyboard.destory();

- Goto main method
-> Write initGame(); // under initGl();

- Create initGame method // under main method

-Create new class called Game
-> Create getInput method
-> Create update method
-> Create render method

- Goto Main class
-> create new instance of the Game class (private static Game game;)

- Goto initGame method
-> Write game = new Game();

- Goto update method
-> Write game.getInput();

- Goto render method
-> Remove //draw <- the placeholder comment from before
-> Write game.render();

- Close Game class

- Run Main class // have a sigh of relief that something worked after coding this much (if a blank window pops up)

- Create GameObject class
-> Add 'abstarct' between public and class // we're only 21:53 into the video :o
-> Create private float x // felt cute might delete these later - Benny
-> Create private float y
-> Create private float sx // size of x
-> Create private float sy // size of y
-> Create private Animation anim // there will be an error for Animation, don't worry about it

- Create update method
- Create render method
-> Create getters for the size floats (x, y, sx, and sy) // Write return statments for all of them too
 
- Create Animation class in engine package // remove previous error for Animation
-> Create global variable private ArrayList<Sprite> frames // there will be an error for Sprite
-> Create global variable private int curFrame // for current frame

- Create a contstructor for the Animation class
-> Write frames = new ArrayList<Sprite>();
- Create render method
- Replace "Sprite" with "Frame" // making frames an arraylist of Frames

- Create Frame class

- Goto Animation class
-> Write frames.get(curFrame); in render method
-> Write temp.render();

- Goto Frame class
-> Create render method

- Goto Animation class
-> Make temp.render the condition of a if statement
--> Write curFrame++; // inside if statement

- Goto Frame class
-> Change void in render method with boolean

- Goto Animation class
-> Create a new Frame object named temp equal to frames.get(curFrame); // frames.get(curFrame) we wrote before
-> Write curFrame %= frames.size(); in if statement

- Goto Frame class
-> Declare new private int length
-> Declare new private Sprite spr
-> Create a constructor with parameters Sprite spr, int length
--> Instantiate this.spr = spr
--> Instantiate this.lenth = length
-> Declare new private int numDisplayed // number of frames already displayed
- Goto constructor
-> Instantiate numDisplayed = 0

- Goto render method
-> Write numDisplayed++;
-> Write spr.render(); // above numDisplayed++
-> Create if statement with condition numDisplayed >= length
--> Set numDisplayed = 0
--> Return true

- Create Sprite class
-> Declare new private float r // red value
-> Declare new private float g // green
-> Declare new private float b // blue
-> Create render method
-> Declare private float sx // size x
-> Declare private float sy // size y
-> Import static org.lwjgl.opengl.GL11.*;

- Goto render method
-> Write glBegin(GL_QUADS);

- Goto GameObject class
-> Delete sx and sy and thier getter methods

- Goto render method in Sprite class
-> write glEnd(); // under glBegin
-> Write glVertex2f(0,0); // between glBegin and glEnd
-> Write glVertex2f(0,sy);
-> Write glVertex2f(sx,sy);
-> Write glVertex2f(sx,0); // dimensions of a quadralateral
-> Write glColor3f(r,g,b); // above glBegin

- Create constructor with parameters float r, float g, float b, float sx, float sy
-> Instantiate this.r = r;
-> Instantiate this.b = b;
-> Instantiate this.g = g;
-> Instantiate this.sx = sx;
-> Instantiate this.sy = sy;
// End of Video 1

// Video 2 12/16/2022
- Goto GameObject class
-> Replace private Animation anim with private Sprite spr;
-> Import static org.lwjgl.opengl.GL11.*;

- Goto render method
-> Write spr.render();
-> Write glTranslatef(x,y,0); // above spr.render
-> Write glPushMatrix(); // above glTranslatef
-> Write glPopMatrix(); // below spr.render

- Create new Physics class
-> Create checkCollision method, return type boolean, parameters GameObject go1, GameObject go2
--> Return false // placeholder

- Goto Sprite class
-> Create setter and getter methods for sx and sy

- Goto GameObject class
-> Create getter methods for the sx and sy of Sprite spr

- Goto checkCollision method in Physics class
-> Declare new Rectangle r1 
-> Import java.awt.Rectangle
-> On the same line, Initialize r1 = new Rectangle((int)go1.getX(), (int)go1.getSX(), (int)go1.getY(), (int)go1.getSY()); // makes hitbox for game object
-> Copy r1, change all 1s to 2s // r2 is under r1
-> Return r1.intersects(r2);

- Create new package com.base.game
- Move Game class into com.base.game package // a pop up window pops up and automatically refactors the code(updates the references of the Game class) 

- Create new package com.base.game.gameobject
-> Create new Player class which extends GameObject
--> Import com.base.engine.GameObject
--> Create constructor with parameters float x, float y
---> Initialize this.x = x;
---> Initialize this.y = y;

- Goto GameObject class
-> Replace private to protected for x, y & spr

- Goto constructor in Player class
-> Declare new Sprite temp
-> Declare and Initialize new public static final SIZE = 32 // above constructor as a global variable
-> Initialize temp = new Sprite(0.1f,1f,0.25f,SIZE,SIZE) // same line as when declared
-> Change temp to this.spr

- Goto GameObject class
-> Create new protected class init(x, y, r, g, b, sx, sy)

- Goto Player class
-> Replace code in constructor with init method
-> Create new public class getInput()

- Goto Game class
-> Declare new private ArrayList<GameObject> objects
-> Create constructor for Game class
--> initialize objects
-> Goto update method
--> Create for(GameObject go : objects)
---> go.update()
-> Goto render method
--> Create for(GameObject go : objects) 
---> go.render() 
-> Create new private Player object player
-> Goto constructor
--> Initialize player
--> objects.add(player)

- Goto Player class
-> Goto getInput() method
--> If W is pressed, move up 1
--> If S is pressed, move down 1
--> If A is pressed, move left 1
--> If D is pressed, move right 1
-> Create public getSpeed() method
--> Return 4f
-> Create private move(magX, magY) method
--> x += getSpeed() * magX
--> y += getSpeed() * magY
-> Declare private float level
-> Declare private float xp
-> Goto constructor
--> Initialize level = 1;
--> Initialize xp = 0;
-> Create public method addXP(float amt)
--> xp += amt
-> Declare private int health
-> Goto constructor
--> Initialize health = 10
-> Create public method getMaxHealth() 
--> return getLevel() * 10;
-> Create public method getCurrentHealth()
--> int max = getMaxHealth()
--> if(health > max)
---> health = max
--> return health;
-> Create public method getStrength()
--> return getLevel() * 4f
-> Create public method getMagic() 
--> return getLevel() * 4f 
-> Remove level variable
-> Goto getLevel() method
--> return (int)(xp / 50) + 1
-> Create public method update()
--> Print all stats through console
-> Test out if XP works
// END OF VIDEO 2











